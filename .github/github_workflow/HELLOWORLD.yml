name: "hello world workflow"

# when action will be run

on:
  push:
    branches:
      -master
      -production

jobs:
  hello_world
runs-on: ubuntu-latest
steps:
  - name: "print hello world"
    run: echo "hello world"
